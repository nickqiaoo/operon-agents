{
  "name": "operon-example-app-server",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types --no-warnings client.ts",
    "server": "node --experimental-strip-types --no-warnings server.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "operon-agents": "workspace:*"
  }
}
